# iOS Appwrite configuration (optional overrides)
# Copy to ios/TCIDPodcast/Resources/Secrets.plist for local overrides.
# Defaults work for production without Secrets.plist.

APPWRITE_ENDPOINT=https://api.tcidpodcast.com/v1
APPWRITE_PROJECT_ID=tcidpodcast
APPWRITE_DATABASE_ID=tcid
